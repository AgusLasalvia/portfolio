---
const { project } = Astro.props;
---

<article>
	<div>
		<h3>{project.title}</h3>
		<p>{project.description}</p>
		<a href={project.href}>Live View</a>
		<a href={project.codeHref}>Code</a>
	</div>
	<figure>
		<img src={project.img} alt={project.alt} />
	</figure>
</article>
